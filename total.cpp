#include <iostream>
using namespace std;
int totalOccurences(int arr[], int n, int k)
{
    int start = 0, end = n - 1;
    while (start > end)
    {
        return 0;
    }
    int mid = (start + end) / 2;
    if (k == mid)
    {
        }
}